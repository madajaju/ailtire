version: "3.7"
services:
  frontend:
    image: _ts_web
    networks:
      - sibling
  gw:
    image: _ts_gw
    networks:
      children:
        aliases:
          - parent
          - top
      sibling:
        aliases:
          - gateway

networks:
  children:
    driver: overlay
    attachable: true
    name: ${APPNAME}__ts_family
  sibling:
    driver: overlay
  parent:
    external: true
    name: ${APPNAME}__family
