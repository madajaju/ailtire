version: "3.7"
services:
  frontend:
    image: _pa_web
    networks:
      - sibling
  gw:
    image: _pa_gw
    networks:
      children:
        aliases:
          - parent
          - top
      sibling:
        aliases:
          - gateway

networks:
  children:
    driver: overlay
    attachable: true
    name: ${APPNAME}__pa_family
  sibling:
    driver: overlay
  parent:
    external: true
    name: ${APPNAME}__family
