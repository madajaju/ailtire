version: "3.7"
services:
  frontend:
    image: pa_a_web
    networks:
      - sibling
  gw:
    image: pa_a_gw
    networks:
      children:
        aliases:
          - parent
          - top
      sibling:
        aliases:
          - gateway

networks:
  children:
    driver: overlay
    attachable: true
    name: ${APPNAME}_pa_a_family
  sibling:
    driver: overlay
  parent:
    external: true
    name: ${APPNAME}_pa_family
