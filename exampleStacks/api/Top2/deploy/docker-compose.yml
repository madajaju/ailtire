version: "3.7"
services:
  frontend:
    image: examplestacks_t_web
    networks:
      - sibling
  gw:
    image: examplestacks_t_gw
    networks:
      children:
        aliases:
          - parent
          - top
      sibling:
        aliases:
          - gateway

networks:
  children:
    driver: overlay
    attachable: true
    name: ${APPNAME}__t_family
  sibling:
    driver: overlay
  parent:
    external: true
    name: ${APPNAME}__family
