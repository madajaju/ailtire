version: "3.7"
services:
  frontend:
    image: sdi_sds_web
    networks:
      - sibling
  gw:
    image: sdi_sds_gw
    networks:
      children:
        aliases:
          - parent
      sibling:
        aliases:
          - gateway

networks:
  children:
    driver: overlay
    attachable: true
    name: ${APPNAME}_sdi_sds_family
  sibling:
    driver: overlay
  parent:
    external:
      name: ${APPNAME}_sdi_family
